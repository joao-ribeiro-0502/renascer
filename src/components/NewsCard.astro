---
// Card de notícia — usado nas listagens e na home.
import type { NewsItem } from '../data/types';
import { formatDate } from '../utils/format';

interface Props {
  item: NewsItem;
  /** Destino do card; padrão é a página da notícia. A home usa '#' (páginas inativas). */
  href?: string;
}
const { item, href = `/noticias/${item.slug}` } = Astro.props;
---

<a class="content-card" href={href}>
  <div class="content-card__media">
    <img src={item.image} alt={item.imageAlt} loading="lazy" decoding="async" />
  </div>
  <div class="content-card__body">
    <p class="content-card__meta">
      <span class="tag tag--red">{item.category}</span>
      <time datetime={item.date}>{formatDate(item.date)}</time>
    </p>
    <h3 class="content-card__title">{item.title}</h3>
    <p class="content-card__excerpt">{item.excerpt}</p>
  </div>
</a>

<style>
  /* Data sempre em linha própria: títulos alinhados entre cards de qualquer
     tamanho de data (evita a data "colada" ao chip quando o texto é curto). */
  .content-card__meta time {
    flex-basis: 100%;
  }
</style>
